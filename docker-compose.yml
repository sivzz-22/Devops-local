version: '3'

services:
  app:
    build: .
    container_name: test_app
    ports:
      - "3000:3000"
    depends_on:
      - db

  db:
    image: mongo:latest
    container_name: test_db
    ports:
      - "27017:27017"